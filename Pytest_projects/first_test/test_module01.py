def test_a1():
    assert 5 + 5 == 10


def test_a2():
    assert 9 // 5 == 1, "Fail test intentionally"
# pytest -v (verbose mode gives the test results for every module)
# for more commands docs.pytest.org/en/latest/cache/html
